/// Core 모듈 - 앱 전반에서 사용하는 공통 기능
class CoreModule {
  static const String name = 'Core';
  static const String version = '1.0.0';
  
  /// 모듈 초기화
  static Future<void> initialize() async {
    // Core 모듈 초기화 로직
    // 예: 로깅, 에러 핸들링, 설정 등
  }
  
  /// 모듈 정리
  static Future<void> dispose() async {
    // Core 모듈 정리 로직
  }
}
